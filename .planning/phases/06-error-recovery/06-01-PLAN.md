---
phase: 06-error-recovery
plan: 01
type: execute
wave: 1
autonomous: true
files_modified:
  - src/services/errorRecoveryService.js
  - src/main/ipc/pool.js
  - src/renderer/poolRenderer.js
---

<objective>
Add error recovery service that classifies pool account errors and applies appropriate recovery strategies. Add UI indicators for recovery status.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create ErrorRecoveryService</name>
  <files>src/services/errorRecoveryService.js</files>
  <action>
    Create src/services/errorRecoveryService.js with:

    Error classification based on last_error text:
    - quota/token exhaustion → cooldown (wait 1h)
    - 401/unauthorized → token refresh needed
    - 403/forbidden → banned → permanent disable
    - timeout → retry immediately
    - unknown → cooldown (wait 5min)

    Recovery strategies:
    - refreshTokens(accountId) - for auth errors, attempt token refresh
    - cooldownRelease(accountId) - for cooldown accounts past cooldown_until, set back to available
    - disableAccount(accountId) - for banned accounts

    Export: class ErrorRecoveryService with recoverable account scanning and recovery execution.
  </action>
</task>

<task type="auto">
  <name>Task 2: Add recovery IPC handlers and UI</name>
  <files>src/main/ipc/pool.js, src/renderer/poolRenderer.js</files>
  <action>
    Add IPC handler `pool-recover-accounts` that:
    1. Scans for error/cooldown accounts past their cooldown period
    2. Classifies errors
    3. Applies recovery strategy
    4. Returns recovery results

    Add "恢复检查" button to pool UI stats area that triggers recovery scan.
  </action>
</task>

</tasks>

<verification>
1. Module loads without errors
2. grep -c "pool-recover" src/main/ipc/pool.js >= 1
3. Tests pass
</verification>
