---
phase: 07-dashboard
plan: 01
type: execute
wave: 1
autonomous: true
files_modified:
  - src/renderer/dashboardRenderer.js
  - renderer.js
  - index.html
  - css/views/dashboard.css
---

<objective>
Create a global dashboard view showing pool overview (total, available, error, disabled), gateway status, and recent error events. Accessible from sidebar as the first view users see.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboardRenderer.js</name>
  <files>src/renderer/dashboardRenderer.js</files>
  <action>
    Create DashboardManager class with:
    - init(): calls render()
    - render(): fetches pool stats and gateway status, renders dashboard
    - _renderPoolOverview(): shows pool totals (total, available, error, disabled, cooldown)
    - _renderGatewayStatus(): shows gateway server status (running/stopped), channel count
    - _renderRecentErrors(): shows accounts with error/cooldown status from pool

    Uses pool-get-accounts and gateway-status IPC calls.
  </action>
</task>

<task type="auto">
  <name>Task 2: Wire dashboard into app</name>
  <files>renderer.js, index.html, css/views/dashboard.css</files>
  <action>
    - Add require for dashboardRenderer in renderer.js
    - Add to windowExports mounting array
    - Add switchView handler for 'dashboard'
    - Add nav button "全局概览" at top of sidebar
    - Add view container in index.html
    - Create css/views/dashboard.css
  </action>
</task>

</tasks>

<verification>
1. Module loads
2. grep checks pass
3. Tests pass
</verification>
